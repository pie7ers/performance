{
  "name": "performance",
  "version": "1.0.0",
  "description": "framework to execute performance test with K6",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "make-env": "ts-node utils/makeEnv.ts"
  },
  "author": "Sebastian Iguavita Roa",
  "license": "ISC",
  "dependencies": {
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "@commitlint/cli": "^18.0.0",
    "@commitlint/config-conventional": "^18.0.0"
  }
}
